{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**/*",
        "*.tsbuildinfo",
        ".wrangler/**/*"
      ]
    },
    "lint": {
      "dependsOn": ["^lint", "build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^test", "build"],
      "outputs": ["coverage/**/*"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
