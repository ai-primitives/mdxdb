{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**/*",
        "*.tsbuildinfo"
      ]
    },
    "lint": {
      "dependsOn": ["^lint", "build"]
    },
    "test": {
      "dependsOn": ["^test", "build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
